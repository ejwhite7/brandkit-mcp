/**
 * @file mcp.ts
 * @description Argument types for BrandKit MCP tools.
 *
 * V2 tools inline their arg types directly (using BrandContext from
 * design-system.ts). This file is retained as a placeholder; all v1
 * interfaces have been removed as part of the v2 migration.
 */

// V2 tool arg interfaces live inline in each tool module.
// See src/tools/*.ts for per-tool types.
