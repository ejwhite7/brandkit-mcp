// Vercel discovers functions under api/. The implementation is built from
// src/adapters/vercel.ts so the same artifact is shipped in the npm package.
export { default } from '../dist/adapters/vercel.js';
